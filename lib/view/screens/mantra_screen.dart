import 'package:flutter/material.dart';

class MantraScreen extends StatelessWidget {
  const MantraScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Mantra Screen", style: TextStyle(
        fontSize: 25,
      ),),
    );
  }
}